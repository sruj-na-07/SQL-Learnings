SELECT patient_id,
UPPER(name) AS name,
LOWER(service) AS service,
CASE WHEN age >= 65
THEN 'Senior'
WHEN age >= 18 AND age < 65
THEN 'Adult'
ELSE 'Minor'
END AS age_category,
LENGTH(name) AS name_length
FROM patients
WHERE LENGTH(name) > 10;
